import Link from 'next/link';import {notFound} from 'next/navigation';import {Shell,PageHero} from '../../components/site';import {getResourceGuide,resourceGuides} from '../../data/resources';
export function generateStaticParams(){return resourceGuides.map(x=>({slug:x.slug}))}
export default async function ResourceGuidePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const guide=getResourceGuide(slug);if(!guide)notFound();return <Shell><PageHero eyebrow={`Resource · ${guide.audience}`} title={guide.title} intro={guide.intro}><div className="hero-actions"><Link className="btn primary" href="/contact">Ask about this</Link><Link className="btn dark" href="/resources">All resources</Link></div></PageHero><section className="section"><div className="wrap"><div className="demo-banner">GENERAL GUIDANCE · This demo resource is for workplace information and does not replace individual clinical, legal or health-and-safety advice.</div><div className="grid2 mt40">{guide.sections.map((section,i)=><article className="card" key={section.title}><span className="badge">{String(i+1).padStart(2,'0')}</span><h2 className="mt14">{section.title}</h2><p>{section.body}</p><ul className="detail-list">{section.points.map(p=><li key={p}>✓ {p}</li>)}</ul></article>)}</div><div className="cta-panel"><div><h2>Need help applying this at work?</h2><p>Tell us about the organisation, workforce and issue and we can point you towards the appropriate occupational-health route.</p></div><Link className="btn primary" href="/what-do-i-need">Tell us what you need</Link></div></div></section></Shell>}