import Link from 'next/link';import {notFound} from 'next/navigation';import {Shell,PageHero} from '../components/site';import {getService,services} from '../data/services';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=getService(slug);if(!s)notFound();return <Shell><PageHero eyebrow="Occupational health service" title={s.title} intro={s.intro}><div className="hero-actions"><Link className="btn primary" href="/contact">Enquire about this service</Link><Link className="btn dark" href="/what-do-i-need">Not sure what you need?</Link></div></PageHero><section className="section"><div className="wrap detail-layout"><div><span className="kicker">Who it is for</span><h2>Designed around the work and the risk.</h2><ul className="detail-list">{s.forWho.map(x=><li key={x}>✓ {x}</li>)}</ul></div><aside className="card detail-aside"><span className="badge">Demo guide price</span><div className="price">{s.price}</div><p>Final pricing depends on scope, volume, location and delivery model.</p><Link className="btn primary" href="/contact">Request a quote</Link></aside></div></section><section className="section band"><div className="wrap"><span className="kicker">What is included</span><h2>A clear, governed service from booking to outcome.</h2><div className="grid2">{s.includes.map((x,i)=><div className="card" key={x}><span className="badge">{String(i+1).padStart(2,'0')}</span><h3>{x}</h3><p>Delivered proportionately to the role, exposure and agreed occupational-health protocol.</p></div>)}</div></div></section><section className="section"><div className="wrap"><span className="kicker">How it works</span><h2>What happens next.</h2><div className="process-grid">{s.process.map((x,i)=><div className="process-step" key={x}><b>{i+1}</b><h3>{x}</h3></div>)}</div><h2 className="mt40">What you receive</h2><ul className="detail-list">{s.outputs.map(x=><li key={x}>✓ {x}</li>)}</ul></div></section><section className="section band"><div className="wrap"><span className="kicker">Questions</span><h2>Frequently asked questions</h2><div className="grid2">{s.faqs.map(([q,a])=><div className="card" key={q}><h3>{q}</h3><p>{a}</p></div>)}</div><div className="cta-panel"><div><h2>Ready to arrange {s.title.toLowerCase()}?</h2><p>Tell us about your workforce and we will shape the right delivery route.</p></div><Link className="btn primary" href="/contact">Start an enquiry</Link></div></div></section></Shell>}
