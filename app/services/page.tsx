import Link from 'next/link';import {Shell,PageHero} from '../components/site';import {services} from '../data/services';export default function Services(){return <Shell><PageHero eyebrow="Services" title="Occupational health without the maze." intro="Choose a service directly or tell us about the workplace issue and we will guide you to the right route."/><section className="section"><div className="wrap grid3">{services.map(s=><article className="card visual-card" key={s.slug}><div className="art">{s.icon}</div><div className="card-body"><span className="badge">Service</span><h3>{s.title}</h3><p>{s.strap}</p><div className="price">{s.price}</div><div className="muted">Representative demo price</div><Link className="btn dark mt14btn" href={`/${s.slug}`}>Full service details</Link></div></article>)}</div></section></Shell>}
