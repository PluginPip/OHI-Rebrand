import Link from 'next/link';import {Shell,PageHero} from '../components/site';import {services} from '../data/services';
function Grid({future=false}:{future?:boolean}){return <div className="grid3 services-grid">{services.filter(s=>(s.status==='future-capability')===future).map(s=><article className="card visual-card" key={s.slug}><div className="art">{s.icon}</div><div className="card-body"><span className="badge">{future?'Future capability':'Service'}</span><h3>{s.title}</h3><p>{s.strap}</p><div className="price">{s.price}</div><div className="muted">{future?'Proposed scope · not currently offered':'Representative demo price'}</div><Link className="btn dark mt14btn" href={`/${s.slug}`}>Full page details</Link></div></article>)}</div>}
export default function Services(){return <Shell><PageHero eyebrow="Services" title="Occupational health without the maze." intro="Explore the current demo service catalogue and the additional capabilities being designed into the future platform. Proposed services are clearly separated from services represented as current."/><section className="section"><div className="wrap"><span className="kicker">Core service catalogue</span><h2 className="mt14">Detailed services, workflows and outcomes.</h2><Grid/></div></section><section className="section band"><div className="wrap"><span className="kicker">Future service expansion</span><h2 className="mt14">Capabilities worth building into the platform now.</h2><p className="lead">These pages define the digital and operational shape of potential future services. They are not claims that OHI currently provides them.</p><Grid future/></div></section></Shell>}
